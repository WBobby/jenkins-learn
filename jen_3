pipeline{

    agent any

    stages{
        stage ('Build'){
            steps{
                println 'Build Start...'
            sh 'ls -al'
            }
            
        }

        stage ('Test'){
            steps{
                println 'Test start...'
                sh "echo ${JAVA_HOME}"
            }            
        }
    }
}