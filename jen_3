pipeline{

    agent any

    stages{
        stage ('Build'){
            println 'Build Start...'
            sh 'ls -al'
        }

        stage ('Test'){
            println 'Test start...'
            sh "echo ${JAVA_HOME}"
        }
    }
}